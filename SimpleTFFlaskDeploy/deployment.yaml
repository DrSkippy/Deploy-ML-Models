apiVersion: apps/v1
kind: Deployment
metadata:
  name: tf-earnings-model-service
  labels:
    app: tf-earnings-model-service
    tier: backend
    version: v1
spec:
  selector:
    matchLabels:
      app: model-tf-earnings-model-service
  replicas: 2
  template:
    metadata:
      labels:
        app: model-tf-earnings-model-service
    spec:
      containers:
      - name: model-service
        image: localhost:32000/tf-earnings-model-service
        ports:
        - containerPort: 8084
        env:
        - name: APP_NAME
          value: model-tf-earnings-model-service
